FROM rust:1.88-slim

RUN apt-get update \
    && apt-get install -y --no-install-recommends \
       pkg-config libsqlite3-dev libssl-dev curl \
    && rm -rf /var/lib/apt/lists/*

RUN cargo install cargo-watch

WORKDIR /app
